{
  "name": "@xevos/platforms",
  "version": "0.1.0",
  "description": "Obscura-backed automation wrappers for stubborn platforms (Twitter/X, LinkedIn): actions plus polling-based synthetic webhooks. Framework-agnostic; the core runtime wraps these into tools and bus events.",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test"
  },
  "license": "MIT",
  "dependencies": {
    "puppeteer-core": "^25.2.1"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
